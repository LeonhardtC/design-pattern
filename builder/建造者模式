建造者模式：
建造者模式（Builder Pattern）使用多个简单的对象一步一步构建成一个复杂的对象。它提供了一种创建对象的最佳方式。

使用场景：
构建一个复杂对象，并对参数作限定

好处：
1.有默认值，开箱即用，方便对象创建
2.能对参数进行限定

与工厂模式区别：工厂模式是批量产生简单对象，而建造者更倾向于创建单个复杂对象，私人定制

例子：框架一般从configration获取参数，默认从classpath下读取文件，再build/create生成